/**
 * Default: offline / local demo only. For multiplayer with friends, copy
 * config.example.js to config.js and set useFirebase: true + your keys.
 */
window.__SKETCH_DUEL_CONFIG = {
  useFirebase: false,
  firebase: {}
};
